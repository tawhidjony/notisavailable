import { Box } from "@mui/material"
import CreateCenterList from "components/Admin/Center/LearningCenter/Add"

type Props = {}

const AddCenterList = (props: Props) => {
  return (
    <Box><CreateCenterList /></Box>
  )
}

export default AddCenterList